<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.2.xsd">

	<bean id="mailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl">
        <property name="protocol" value="${app.mail.protocol}" />
		<property name="host" value="${app.mail.host}" />
        <property name="port" value="${app.mail.port}" />
        <property name="username" value="${app.mail.username}" />
        <property name="password" value="${app.mail.password}" />
        <property name="javaMailProperties">
            <props>
                <prop key="mail.smtp.auth">${app.mail.smtp.auth}</prop>
                <prop key="mail.smtp.starttls.enable">${app.mail.smtp.starttls.enable}</prop>
            </props>
        </property>
	</bean>

	<bean id="mailService" class="ro.cloudSoft.cloudDoc.spring.services.SpringMailService">
		<property name="mailSender" ref="mailSender" />
		<property name="fromAddress" value="${app.mail.from}" />
	</bean>
	
	<bean id="outOfOfficeMailService" class="ro.cloudSoft.cloudDoc.services.mail.WebServiceBasedOutOfOfficeMailService">	
		<property name="wsdlLocation" value="${app.external.web_services.out_of_office_mail.wsdl_location}" />
		
		<property name="enabled" value="${app.external.web_services.out_of_office_mail.is_enabled}" />
	
		<property name="dateFormat" value="${app.external.web_services.out_of_office_mail.date_format}" />
		
		<property name="operationResponseOk" value="${app.external.web_services.out_of_office_mail.operation_response.ok}" />
		<property name="operationResponseError" value="${app.external.web_services.out_of_office_mail.operation_response.error}" />
	</bean>

</beans>